{
  "name": "pay-stripe",
  "version": "1.0.0",
  "description": "react native app for stripe payments",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "eleven",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "stripe": "^12.3.0"
  }
}
